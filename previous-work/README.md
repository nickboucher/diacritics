# Diactritics Previous Work Comparison

This directory contains a Notebook and associated Docker container that compares the performance of the Diactritic attacks with previous work.

To start the notebook, run the following command:
```bash
./start.sh
```
This will display a URL that you can open in your browser to access the notebook.