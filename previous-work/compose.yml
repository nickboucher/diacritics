name: diacritics-previous-work

services:
  jupyter:
    build: .
    ports:
      - 8888:8888
    volumes:
      - ./notebooks:/app/notebooks
    command: jupyter lab --allow-root --no-browser --ip 0.0.0.0 --port 8888